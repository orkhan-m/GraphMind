# Gateway API key from https://thegraph.com/studio/ (used to authenticate with the Subgraph MCP server)
GATEWAY_API_KEY=

# OpenAI API key used to power the reasoning agent
OPENAI_API_KEY=

# Optional: override the model used by the agent
OPENAI_MODEL=gpt-4o
